def solution(s):
    ans = s.split(' ')
    answer = list(map(int,ans))
    answer.sort()
    
    #sort로 s에 있는 숫자들 정리 해 놓고 최소, 최대 출력
    return str(answer[0]) +' '+ str(answer[-1])

# 다른 사람의 풀이
def solution(s):
    s = list(map(int,s.split()))
    return str(min(s)) + " " + str(max(s))
