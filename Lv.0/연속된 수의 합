def solution(num, total):
    if num % 2 == 1:
        answer = [int(a) for a in range((total//num)-num//2,(total//num)+num//2+1)]
    else:
        answer = [int(a) for a in range((total//num)-num//2+1,(total//num)+num//2+1)]
    return answer
