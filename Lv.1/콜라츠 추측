def solution(num):
    n = 0
    for n in range(501):
        if num != 1:
            if num % 2 == 0:
                num = num / 2
                n = n + 1
            else:
                num = num*3 +1
                n = n + 1
        else:
            return n
    else:
        return -1
